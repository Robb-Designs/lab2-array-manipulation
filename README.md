# Functions, Logic, and Refactoring

## 🧠 My Thought Process

Methods like `push()` and `filter()` made it easier to manage and manipulate arrays. I really like the method `filter()`, it is very useful to search though lists, instead of having to write a loop. With `filter()` I can return a new array only containing items that match the search term. 


## For the Future

The next time I solve a problem like this, i'd like to include some edge cases. For example, id like to update the interface by clearing ot the input fiel after the user clicks the button to add item.